import styled from "@emotion/styled";

const NewPageTitle = styled.span`
  font-weight: 500;
  font-size: 24px;
  line-height: 115%;
  color: ${(props) => props.theme.palette.text.primary};
  padding-bottom: 20px;
`;

export default NewPageTitle;
