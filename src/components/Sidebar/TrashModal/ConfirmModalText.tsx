import styled from "@emotion/styled";

const ConfirmModalText = styled.p`
  font-weight: 500;
  font-size: 24px;
  line-height: 115%;
  color: ${(props) => props.theme.palette.text.primary};
`;

export default ConfirmModalText;
