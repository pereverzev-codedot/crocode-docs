import { useCallback, useEffect, useState, useContext } from "react";

export type TgetInfo = {
  handlGetInfo: (e: React.MouseEvent<HTMLDivElement, MouseEvent>) => void;
};

export const SidebarItems = () => {
  return <></>;
};
